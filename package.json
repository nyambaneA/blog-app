{
  "name": "blog-app",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "build": "echo 'Building frontend...' && cd frontend && npm ci && npm run build && echo 'Frontend build complete'",
    "start": "cd backend && node server.js"
  }
}