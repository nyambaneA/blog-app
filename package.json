{
  "name": "blog-app",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "build": "cd frontend && PUBLIC_URL=/ npm run build && echo 'Build complete. Checking index.html...' && grep -n '%PUBLIC_URL%' build/index.html && echo 'Copying files...' && mkdir -p ../backend/public && cp -r build/. ../backend/public/",
    "start": "cd backend && node server.js"
  }
}